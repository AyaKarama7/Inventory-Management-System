using Business;
namespace TestSys
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //EmployeeBusiness.Insert("omnia");
            ////WarehouseBusiness.Insert("W1", "A1", 1);
            //WarehouseBusiness.Insert("W2", "A2", 2);
            ////WarehouseBusiness.Update("W1","A3", 1);
            //var list = WarehouseBusiness.SelectAll();
            //foreach (var item in list) Console.WriteLine($"{item.WarehouseName}, {item.WarehouseAddress}, " +
            //    $"{item.WarehouseMangerId}");
        }
    }
}
